[user]
    name = Dmitry Medvinsky
    email = dmedvinsky@gmail.com
[help]
    autocorrect = -1
[core]
    editor = vim
    pager = less -FRSX
    whitespace = fix,trailing-space,space-before-tab,cr-at-eol
[color]
    ui = auto
    diff = auto
    status = auto
    log = auto
    grep = auto
    interactive = auto
    pager = true
[color "diff"]
    whitespace = blue reverse
    meta = white
[apply]
    whitespace = fix
[diff]
    renames = copies
    tool = vimdiff-g
[merge]
    stat = true
    conflictstyle = diff3
    tool = threesome
[difftool "vimdiff-g"]
    cmd = "vim -g --role diff -d -f $LOCAL $REMOTE"
[mergetool "threesome"]
    cmd = "vim -g --role merge -f $BASE $LOCAL $REMOTE $MERGED -c 'ThreesomeInit'"
    trustExitCode = false
    keepBackup = false
[difftool]
    prompt = false
[mergetool]
    prompt = false
[advice]
    pushNonFastForward = false
    statusHints = false
[branch]
    autosetupmerge = true
[push]
    default = tracking
